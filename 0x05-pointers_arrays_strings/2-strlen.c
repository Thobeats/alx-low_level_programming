#include <stdio.h>

/**
 * _strlen - swaps the values of two integers
 *
 * @s: the string
 *
 * Return: the length of the string
 *
 */


int _strlen(char *s)
{
	int length;
	char sm;

	sm = s;

	length = strlen(sm);

	return (length);
}
