#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int _putchar(char c);
int calc_change(int *change, int cents);
int _myname(int argc, char *argv[]);
#endif /* MAIN_H */
